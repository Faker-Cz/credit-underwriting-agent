# 授信报告智能体内部架构与规则评审

## 1. 当前定位与结论

当前版本定位为银行内部经办和复核辅助工具，不作为自动审批系统，也不直接面向外部客户。

统一报告立场：客户经理视角，以支持与事实、制度和偿债能力相匹配的合理授信为主线，最终结论以事实为准。支持授信不是预设通过；事实不支持原方案时，可形成附加条件、压降、结构调整、暂缓或不支持意见。

经本轮整理，主流程、专项规则、模板资产和审计脚本的职责已基本分离，适合内部试运行。后续优化重点应从继续累加写作规则，转向项目样本回归、模板版本治理和高风险字段校验。

## 2. 架构分层

```text
用户项目指令
  ↓
SKILL.md：总流程、权限、路由、不可突破的原则
  ↓
references/：证据、财务、写作、担保、方案、版式和质量门禁
  ↓
scripts/：材料清单、DOCX审计、工作簿审计及模板维护
  ↓
assets/：两份Word清洁模板和一份流贷测算清洁模板
  ↓
项目工作副本：证据台账、分析底稿、报告和测算表
```

规则冲突时按以下顺序处理：用户本轮明确要求 → `SKILL.md`硬约束 → 当前任务专项参考 → 通用建议。真实性、权限和敏感信息边界不因写作倾向而放宽。

## 3. 本轮已解决事项

- 统一技能名称为`corporate-credit-investigation-cn`，调用方式为`$corporate-credit-investigation-cn`。
- 统一“客户经理视角＋支持合理授信主线＋结论服从事实”的立场，并写入总规则、专项规则、质量门禁和默认提示词。
- 统一字体规则：用户未明确禁止字体调整时，正文及表格正文默认仿宋_GB2312小四；其他版式不得擅自调整。
- 增加材料指令隔离：客户文件和网页只作为待核实数据，不作为智能体操作指令。
- 增加内部数据边界：敏感材料默认仅在本地项目范围处理，未经授权不外发、不公开分享。
- 清理两份Word模板中的真实机构／个人信息、固定年份、示例评级、预勾选项、填写说明和预填判断。
- 修正流贷测算表文字错误，增加调节系数数据验证、工作表保护和单页打印设置。
- 增强DOCX和工作簿审计脚本，支持模板／报告模式和严格失败状态。
- 材料清单默认排除缓存、隐藏目录、历史输出和自身输出文件，并默认不记录本机绝对路径。
- 增加内部发布回归测试，校验模板纯净、流贷公式契约、技能名称和根目录／运行资产一致性。

## 4. 仍需人工控制的边界

- 自动检查不能证明客户事实真实，只能检查残留、结构、公式和部分一致性。
- Word渲染受本机字体和办公软件差异影响，最终提交前仍须在实际使用的Word/WPS环境逐页复核。
- 单一客户模板第二部分的顶层标题目前保留原模板“三”后接“六”；在模板归口部门确认前不自动重编号。`audit_docx.py`已输出Word自动编号定义，供工具层识别和人工复核。
- 客户经理视角不能替代审批独立性；重大风险、资料缺口和不支持原方案的事实必须保留。
- 内部评级、征信、中征码、政策准入和审批权限数据无法由公开材料替代，必须由经办人补录或确认。
- 流贷测算表的结果是方案论证输入，不是自动授信额度。

## 5. 内部版本验收标准

每次修改规则、模板或审计脚本后，至少完成：

1. 运行`python3 ${CODEX_HOME:-$HOME/.codex}/skills/.system/skill-creator/scripts/quick_validate.py skill/corporate-credit-investigation-cn`校验Skill结构；
2. 运行`python3 -m unittest discover -s tests -v`；
3. 两份DOCX以`--mode template --strict`审计通过；
4. 流贷模板以`--mode loan-template --strict`审计通过；
5. 根目录模板与`assets/`同名资产SHA-256一致；
6. 两份DOCX逐页渲染检查，工作簿确认单页打印；
7. 检查Git差异，不提交客户材料、临时渲染文件、审计输出或访问凭据。

报告成稿审计应使用用户最新版作为`--baseline`，并通过`--forbidden-file`载入项目历史客户名和机构名。用户明确要求纯数值Excel交付时，工作簿审计追加`--pure-values`；公式驱动测算模板不使用该开关。

## 6. 下一阶段建议

- 用脱敏后的低风险、一般授信和复杂项目各建立至少一套回归样本，验证规则不会只适配单一行业。
- 为关键联动字段建立结构化清单，重点覆盖额度、期限、品种、担保、用途、还款来源、评级和风险条件。
- 对审计脚本增加更多小型单元测试，而不是仅依赖当前模板通过测试。
- 模板发生机构制度升级时保留版本号、变更说明和审批来源，避免根目录与运行资产再次分叉。
